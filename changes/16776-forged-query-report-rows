- Fixed the osquery result log endpoint so that an enrolled host can no longer inject rows into the report of a saved query it was never scheduled to run. Results are now only stored when the reported query is part of the host's schedule (belongs to the host's fleet, is in the query's label scope, and is actually on a schedule) instead of being trusted based on the reported query name alone.
